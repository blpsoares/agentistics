# Briefs — the owner's original text

These three files are the brief the runtime specification was written from, committed **verbatim**
and therefore in Portuguese (the one exception to this repository's English-only rule: translating
them would change what was asked). The specifications cite them — `execucao.md`'s principle #20 is
quoted in `specs/2026-09-20-delivery-breakdown.md` — and a citation to a file outside the repository
is a dangling reference.

- `Completo.md` — the discovery + architecture brief (71 sections).
- `execucao.md` — the operational rules for executing the work through the ALM.
- `mermaid_completo.mmd` — the architecture diagram proposed alongside the brief.

The research the brief commissioned is in `../research/`.
